namespace ParkingManagement.src.domain.Entities
{
    // ASK - This could separate tabel in DB. Then we will not have to change enum to add new vehicle type.
    public enum VehicleType
    {
        SmallCar,
        MediumCar,
        LargeCar
    }
}
